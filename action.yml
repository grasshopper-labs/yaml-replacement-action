name: 'YAML values replacement'
description: 'Replace specific value/s in YAML file'
inputs:
  filePath:
    description: 'List of folders'
    required: true
  keyValueStr:
    description: 'Keys to replace in the YAML file'
    required: true
runs:
  using: 'node20'
  main: 'dist/index.js'
